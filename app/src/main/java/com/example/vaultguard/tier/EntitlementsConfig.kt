package com.example.vaultguard.tier

/**
 * Backend base URL for Sprint 0.1.2 entitlements stub.
 *
 * For Android emulator, 10.0.2.2 maps to host machine localhost.
 * For physical device, you must replace with your LAN IP or real domain.
 */
object EntitlementsConfig {
    const val BASE_URL = "http://10.0.2.2:8000"
}

